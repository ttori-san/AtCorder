const lines=[];
const reader = require('readline').createInterface({
    input: process.stdin,
    output: process.stdout
});
reader.on('line', function (line) {
  lines.push(line);
});
reader.on('close', function () {

  const [n,x,v,p] = lines[0].split[" "].map(Number);
  const oneCup = {
    cup : {
      alc : p,
       ml : v,
    }
  }
  Object(oneCup).for (let i = 0; i < array.length; i++) {

  }
  {
    const each_alc_and_ml = Object.values(cups);
    const amountsOfAlc = OneCupOfAlc
  }
    // この中に入力が終わった後の処理=ロジックを記載する。
});

//ｃｍｄで以下を実行
// yarn ts-node abc189/b.ts 
// (abc191/a.ts部分は適宜変更)